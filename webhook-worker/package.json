{
  "name": "koreanpulse-webhook",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker — Polar webhook + license validation, D1-backed (Lemon Squeezy handler retained as historical reference; LS store application was declined 2026-05-06)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "wrangler d1 execute koreanpulse_db --local --file ./migrations/0001_licenses.sql && wrangler d1 execute koreanpulse_db --local --file ./migrations/0002_pricing_v2.sql",
    "migrate:prod": "wrangler d1 execute koreanpulse_db --remote --file ./migrations/0001_licenses.sql && wrangler d1 execute koreanpulse_db --remote --file ./migrations/0002_pricing_v2.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.78.0"
  }
}
